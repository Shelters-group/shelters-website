# build output
dist/
.output/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.local
# .env.production est volontairement committé : il ne contient que des
# valeurs PUBLIC_* (inlinées dans le bundle client par Astro au build, donc
# visibles publiquement de toute façon). Cloudflare le lit à chaque build.

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# VS Code
.vscode/

# astro
.astro/
